name: osm_tile_viewer
description: A Flutter package for searching places and displaying OpenStreetMap tiles using Nominatim geocoding.
version: 0.1.0
repository: https://github.com/hanzalahcmd/smd_osm_mapviewer_package

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.10.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  # No assets or fonts needed for the core package
